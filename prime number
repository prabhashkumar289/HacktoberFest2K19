#include<stdio.h>
int main()
{
 int num,i,j;
 scanf("%d",&num);
 for(i=1;i<=num;i++)
 {
  int flag=0;
  for(j=2;j<=i/2;j++)
  {
   if(i%j==0)
   flag=1;
   }
   if(flag==0)
   printf("%d",i);
  }
  return 0;
}
  
